server:
  port: 8006
  servlet:
    context-path: /notice
spring:
  application:
    name: hitch-notice-server
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        prefix: taxi-feign-common
        file-extension: yaml
