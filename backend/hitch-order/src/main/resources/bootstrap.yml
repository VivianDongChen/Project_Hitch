server:
  port: 8005
  servlet:
    context-path: /order
spring:
  application:
    name: hitch-order-server
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        prefix: taxi-feign-common
        file-extension: yaml
