server:
  port: 8007
  servlet:
    context-path: /account
spring:
  application:
    name: hitch-account-server
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        prefix: taxi-feign-common
        file-extension: yaml
